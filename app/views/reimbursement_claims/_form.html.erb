<%= form_with(model: [@employee, reimbursement_claim]) do |form| %>
  <% if reimbursement_claim.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reimbursement_claim.errors.count, "error") %> prohibited this reimbursement_claim from being saved:</h2>

      <ul>
        <% reimbursement_claim.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="w-50">



     <div class="form-group py-2">
      <%= form.label :date_of_expenditure, style: "display: block" %>
      <%= form.date_field :date_of_expenditure, class:"form-control" %>
    </div>

    <div class="form-group py-2">
      <%= form.label :amount, style: "display: block" %>
      <%= form.number_field :amount, class:"form-control" %>
    </div>

     <div class="form-group py-2">
      <%= form.label :purpose, style: "display: block" %>
      <%= form.text_area :purpose, class:"form-control" %>
    </div>


  </div>

  <div class="d-flex">
    <%= form.submit class: 'btn btn-primary'%>
    <%= link_to 'Back', employee_reimbursement_claims_path(@employee), class: 'btn btn-secondary mx-3' %>
  </div>

<% end %>
